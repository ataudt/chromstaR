useDynLib(chromstar)

# Export all names
# exportPattern(".")
export(
bed2binned,
bam2binned,
bedGraph2binned,
get.chrom.lengths,

call.peaks.univariate,
prepare.multivariate,
call.peaks.multivariate,
unis2pseudomulti,
split.per.chrom,
merge.chroms,

plot.distribution,
plot.distributions.multi,
plot.distributions.to.pdf,
plot.distribution.normal,
plot.boxplot,
plot.transition,
stateorderByTransition,

export.unihmm2bed,
export.unihmm2wiggle,
export.multihmm2bed,
export.multihmm2wiggle,
export.GRanges2bed,
export.binned2bedGraph,
export.binned2wiggle,
binned2GRanges,
hmm2GRanges,
bed2GRanges,

get.states,
combinatorial.states,
collapse.bins,
dec2bin,
bin2dec,
state.brewer,
mixedsort,
mixedorder,
estimate.RAM.consumption,
get.state.labels,
get.state.colors,

bin.annotation,
enrichment.analysis,
enrichment.from.annotation,
enrichment.from.expression,
correlation.analysis,
gwasCatalog2GRanges,
plot.fractionOfSegmentation,
plot.heatmapFoldEnrichment,
cross.correlation,
summarize,

dzinbinom,
pzinbinom,
qzinbinom,
rzinbinom,

simulate.univariate,
simulate.multivariate,
simulate.binned2sam,
simulate.binned2bed,
find.optimal.binsize,
bernoulli.from.univariate.results,
rescale.univariate.model
)

# Import all packages listed as Imports or Depends
import(
	GenomicRanges, IRanges, Rsamtools
)
