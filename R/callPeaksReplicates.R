#' Fit a multivariate Hidden Markov Model to multiple ChIP-seq replicates
#'
#' Fit an HMM to multiple ChIP-seq replicates and derive correlation measures. Input is a list of \code{\link{chromstaR_univariateHMM}}s generated by \code{\link{callPeaksUnivariate}}.
#'
#' @author Aaron Taudt
#' @param hmm.list A list of \code{\link{chromstaR_univariateHMM}}s generated by \code{\link{callPeaksUnivariate}}, e.g. \code{list(hmm1,hmm2,...)} or \code{c("file1","file2",...)}. Alternatively, this parameter also accepts a \code{\link{chromstaR_multivariateHMM}} and will check if the distance between replicates is greater than \code{max.distance}.
#' @param max.states The maximum number of combinatorial states to consider. The default (32) is sufficient to treat up to 5 replicates exactly and more than 5 replicates approximately.
#' @param force.equal The default (\code{FALSE}) allows replicates to differ in their peak-calls, although the majority will usually be identical. If \code{force.equal=TRUE}, all peaks will be identical among all replicates.
#' @param eps Convergence threshold for the Baum-Welch algorithm.
#' @param max.iter The maximum number of iterations for the Baum-Welch algorithm. The default \code{NULL} is no limit.
#' @param max.time The maximum running time in seconds for the Baum-Welch algorithm. If this time is reached, the Baum-Welch will terminate after the current iteration finishes. The default \code{NULL} is no limit.
#' @param num.threads Number of threads to use. Setting this to >1 may give increased performance.
#' @param max.distance This number is used as a cutoff to group replicates based on their distance matrix. The lower this number, the more similar replicates have to be to be grouped together.
#' @return Output is a \code{\link{chromstaR_multivariateHMM}} object with additional entry \code{replicateInfo}. If only one \code{\link{chromstaR_univariateHMM}} was given as input, a simple list() with the \code{replicateInfo} is returned.
#' @seealso \code{\link{chromstaR_multivariateHMM}}, \code{\link{callPeaksUnivariate}}, \code{\link{callPeaksMultivariate}}
#' @export
callPeaksReplicates <- function(hmm.list, max.states=32, force.equal=FALSE, eps=0.01, max.iter=NULL, max.time=NULL, num.threads=1, max.distance=0.2) {

	## Enable reanalysis of multivariate HMM
	if (class(hmm.list)==class.multivariate.hmm) {

		multimodel <- hmm.list
		if (is.null(multimodel$replicateInfo)) {
			warning("No check done because no replicateInfo was found")
			return(multimodel)
		}
		info.df <- multimodel$replicateInfo$info
		cor.matrix <- multimodel$replicateInfo$correlation
		dist.matrix <- multimodel$replicateInfo$distance
		hc <- hclust(dist.matrix)
		info.df$group <- cutree(hc, h=max.distance)

	### Call peaks for several replicates ###
	} else {

		hmms <- loadHmmsFromFiles(hmm.list)

		## Univariate replicateInfo
		ids <- unlist(lapply(hmms, '[[', 'ID'))
		weight.univariate <- unlist(lapply(hmms, function(x) { x$weights['modified'] }))
		total.reads <- unlist(lapply(hmms, function(x) { sum(x$bins$reads) }))
		info.df <- data.frame(total.reads=total.reads, weight.univariate=weight.univariate)
		rownames(info.df) <- ids

		### Correlation analysis ###
		if (length(hmms) == 1) {
			info.df$weight.multivariate <- NA
			info.df$group <- 1
			cor.matrix <- NA
			dist.matrix <- NA
			multimodel <- list()
		} else if (length(hmms) >= 2) {
			num.states <- min(max.states, 2^length(hmms))
			if (force.equal) {
				states2use <- stateBrewer(rep(paste0('r.',paste(ids, collapse='-')),length(hmms)))
				multimodel <- callPeaksMultivariate(hmms, use.states=states2use, eps=eps, max.iter=max.iter, max.time=max.time, num.threads=num.threads)
				multimodel
			} else {
				states2use <- stateBrewer(paste0('x.', ids))
				multimodel <- callPeaksMultivariate(hmms, use.states=states2use, num.states=num.states, eps=eps, max.iter=max.iter, max.time=max.time, num.threads=num.threads)
			}
			binstates <- dec2bin(multimodel$bins$state, colnames=multimodel$IDs)
			cor.matrix <- cor(binstates)
			weight.multivariate <- apply(binstates, 2, sum) / nrow(binstates)
			info.df$weight.multivariate <- weight.multivariate
			dist.matrix <- dist(cor.matrix)
			hc <- hclust(dist.matrix)
			info.df$group <- cutree(hc, h=max.distance)
		}

		## Make return object
		multimodel$replicateInfo$info <- info.df
		multimodel$replicateInfo$correlation <- cor.matrix
		multimodel$replicateInfo$distance <- dist.matrix

	}

	## Check groups and issue warnings
	num.groups <- length(unique(info.df$group))
	if (num.groups > 1) {
		avg.total.reads <- unlist(lapply(split(info.df, info.df$group), function(x) { mean(x$total.reads) }))
		IDs.keep <- rownames(info.df)[info.df$group==names(avg.total.reads[which.max(avg.total.reads)])]
		IDs.keep.string <- paste(IDs.keep, collapse='\n')
		IDs.throw <- rownames(info.df)[info.df$group!=names(avg.total.reads[which.max(avg.total.reads)])]
		IDs.throw.string <- paste(IDs.throw, collapse='\n')
		string <- paste0("Your replicates cluster in ", num.groups, " groups. Consider redoing your analysis with only the group with the highest average coverage:\n", IDs.keep.string, "\nReplicates from groups with lower coverage are:\n", IDs.throw.string)
		warning(string)
	}

	return(multimodel)

}
